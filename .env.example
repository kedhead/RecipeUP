# Database (Required)
# For local development:
DATABASE_URL="postgresql://username:password@localhost:5432/recipeup"
# For production (Supabase):
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# Authentication (Required)
JWT_SECRET="your-super-secret-jwt-key-min-32-characters-long"

# App Configuration (Required)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"

# Spoonacular API (Optional - for external recipes)
SPOONACULAR_API_KEY="your-spoonacular-api-key"

# Optional: Email service (for email verification)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"

# Optional: File upload service
# UPLOAD_SERVICE="local" # or "s3", "cloudinary"
# AWS_ACCESS_KEY_ID="your-aws-access-key"
# AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
# AWS_REGION="us-east-1"
# AWS_S3_BUCKET="your-bucket-name"